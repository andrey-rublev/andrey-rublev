# Copy to .dev.vars for local development (gitignored).
CF_API_TOKEN=your-zone-analytics-read-token
ZONE_TAG=your-zone-id

# Only for the offline mock test - leave out to hit the real API.
# GRAPHQL_URL=http://127.0.0.1:8788/graphql
